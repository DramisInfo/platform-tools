apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  environment: staging
resources:
  - ../../base
  # - agentic-management-application.yaml  

patches:
  - path: patches/platform-core.yaml
  - path: patches/cert-manager-identity.yaml
