apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  environment: dev
resources:
  - ../../base
  - agentic-management-application.yaml

patches:
  - path: patches/platform-core.yaml
  # - path: patches/github-app-secret-patch.yaml
