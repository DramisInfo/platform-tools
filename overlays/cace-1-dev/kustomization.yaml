apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
commonLabels:
  environment: dev
resources:
  - ../../base
  - crossplane/certmanager-identity.yaml
  # - teams/team-alpha

patches:
  - path: patches/platform-core.yaml
  # clientId is managed by Terraform (written on every cluster provision)
  - path: patches/crossplane-identity.yaml
