# This file is managed by Terraform (home-lab/terraform-provision).
# It is overwritten on every `terraform apply` with the Crossplane UMI client ID
# for this cluster. Do not edit manually.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-core
  namespace: argocd
spec:
  source:
    helm:
      valuesObject:
        bootstrap:
          crossplane:
            workloadIdentity:
              clientId: "00000000-0000-0000-0000-000000000000"
