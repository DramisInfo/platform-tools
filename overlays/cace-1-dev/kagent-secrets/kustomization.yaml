apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - azure-openai-secret.yaml
  # Commented out until kagent CRD is installed
  # - azure-openai-modelconfig.yaml
  - github-app-secret.yaml