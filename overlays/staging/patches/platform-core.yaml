apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-core
  namespace: argocd
spec:
  source:
    targetRevision: "v0.9.3"
    helm:
      valuesObject:
        global:
          clusterName: staging
        bootstrap:
          nats:
            gateway:
              enabled: true
              name: "dev-cluster"
              advertise: "nats.staging.dramisinfo.com:7222"