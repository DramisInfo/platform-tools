apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: media-server
  namespace: argocd
spec:
  project: team-alpha
  source:
    repoURL: https://github.com/DramisInfo/platform-tools
    targetRevision: HEAD
    path: app-teams/team-alpha/charts/media-server
    helm:
      values: |
        replicaCount: 3
  destination:
    server: https://kubernetes.default.svc
    namespace: team-alpha-media-server
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
